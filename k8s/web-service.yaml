apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app.kubernetes.io/instance: web
    app.kubernetes.io/name: web
spec:
  ports:
  - port: 5000
    targetPort: web-port
    protocol: TCP
  selector:
    app: web
