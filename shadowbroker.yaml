version: v12

branches:
  - name: deploy
    environment: dev

kubernetesResources:
  applyGroups:
    podcast_generator:
      index: 1
  tasks:
    - build:
        images:
          - id: webserver
            dockerfile: webserver/DOCKERFILE
            memory: 4Gi
